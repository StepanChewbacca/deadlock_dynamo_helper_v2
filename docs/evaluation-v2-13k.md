# Evaluator V2 13k window

The Contextual V2 evaluator now accepts and defaults to 13,000 matches. The default 70/15/15 split produces 9,100 training, 1,950 validation, and 1,950 untouched final-test matches.

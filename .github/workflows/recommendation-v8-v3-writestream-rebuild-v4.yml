name: Recommendation V8 V3 WriteStream Rebuild V4

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'This one-shot V3 rebuild workflow is retired. Dataset V3 is preserved and must not be rebuilt by this workflow.'

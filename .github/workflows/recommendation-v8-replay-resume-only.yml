name: Recommendation V8 Replay Resume Only

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'This legacy replay resume workflow is retired. Use the PostgreSQL-backed replay workflow.'

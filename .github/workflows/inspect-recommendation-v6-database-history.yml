name: Inspect Recommendation V6 database history

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Database history diagnostic is complete.'

name: Recommendation V8 Replay Streaming Snapshot Recovery V5

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'This one-shot replay snapshot recovery is retired. The immutable candidate snapshot is preserved.'

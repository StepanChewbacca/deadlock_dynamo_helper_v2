name: Recommendation V8 Pipeline Timeout Resume

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'This legacy timeout recovery is retired. Use the PostgreSQL-backed replay workflow.'

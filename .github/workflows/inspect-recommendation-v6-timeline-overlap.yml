name: Inspect Recommendation V6 timeline overlap

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Timeline overlap diagnostic is complete.'

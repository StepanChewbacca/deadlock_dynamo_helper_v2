name: Recommendation V8 Replay Deploy Resume V6

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'This one-shot replay deploy-resume workflow is retired. Use the PostgreSQL-backed replay workflow.'

name: Recommendation V8 Replay Resume Only V7

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'This one-shot replay resume workflow is retired. Use the PostgreSQL-backed replay workflow.'
